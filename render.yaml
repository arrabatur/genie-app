services:
  - type: web
    name: genie-app-server
    runtime: node
    buildCommand: cd server && npm install
    startCommand: node server/src/index.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PORT
        value: 10000
