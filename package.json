{
  "name": "genie-app",
  "version": "1.0.0",
  "private": true,
  "description": "Génie — PWA + API + app mobile",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "start": "npm start --workspace=server",
    "dev": "npm run dev --workspace=server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix mobile"
  },
  "author": "arrabatur",
  "license": "ISC"
}
